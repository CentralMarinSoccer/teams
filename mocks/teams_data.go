package mocks

/*
import (
	"github.com/centralmarinsoccer/teams/teamsdata"
)

// TeamsData provides a mechanism to set and get mocked TeamsData
type TeamsData struct {
	SaveCall struct {
		Receives struct {
			Filename string
			 }
		Returns struct {
			Error error
			}
		 }
	LoadCall struct {
		Receives struct {
			Filename string
		}
		Returns struct {
			Error error
		}
	}
	ToJSONCall struct {
		Returns struct {
			JSON string
		}
	}
	ToMapCall struct {
		Returns struct {
			Map map[string]string
		}
	}
}

// Save provides a mechanism to mock the call
func (t *TeamsData) Save(filename string, config *teamsdata.ConfigTeams) error {
	t.SaveCall.Receives.Filename = filename
	return t.SaveCall.Returns.Error
}

// Load provides a mechanism to mock the call
func (t *TeamsData) Load(filename string, config *teamsdata.ConfigTeams) error {
	t.LoadCall.Receives.Filename = filename
	return t.LoadCall.Returns.Error
}

// ToJSON provides a mechanism to mock the call
func (t *TeamsData) ToJSON() string {
	return t.ToJSONCall.Returns.JSON
}

// ToMap provides a mechanism to mock the call
func (t *TeamsData) ToMap() map[string]string {
	return t.ToMapCall.Returns.Map
}
*/
